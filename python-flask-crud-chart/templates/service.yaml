apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-flask-crud-chart.fullname" . }}
  labels:
    {{- include "python-flask-crud-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-flask-crud-chart.selectorLabels" . | nindent 4 }}
