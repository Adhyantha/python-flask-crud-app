apiVersion: v1

kind: Service

metadata:
  name: python-flask-app-service

spec:
  type: NodePort

  selector:
    app: python-flask-app

  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30004